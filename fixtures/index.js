const newPullRequest = require("./new-pr");
const oldStyleComment = require("./old-style-comment");
const newStyleComment = require("./new-style-comment");
const newStyleCodeReview = require("./new-style-code-review");
const withImage = require("./with-image");

module.exports = {
  newPullRequest,
  oldStyleComment,
  newStyleComment,
  newStyleCodeReview,
  withImage,
};
